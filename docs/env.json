{
  "_note": "Links to the FINISHED PRODUCT per environment, shown in the harness (developer view) top nav. For an application project: local = the app's local dev server, dev/prod = its deployments. rapid-workflow is a methodology/tooling repo, so its only local runtime is the harness/observatory server; dev/prod are unset.",
  "local": {
    "url": "http://localhost:4040",
    "launch": "python3 ~/projects/rapid-workflow/tools/observe-server.py --port 4040"
  },
  "dev": { "url": "" },
  "prod": { "url": "" }
}
