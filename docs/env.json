{
  "_note": "Per-project link config for Atlas (the developer-view harness). product = the finished app per environment; source = the repos; atlas = the deployed (static) Atlas URL. A built project fills these from forge.yaml deploy targets at atlas-init (P1) / atlas-deploy (P9). rapid-workflow is tooling: local = the harness/observatory server; dev/prod/atlas unset.",
  "local": {
    "url": "http://localhost:4040",
    "launch": "python3 ~/projects/rapid-workflow/tools/observe-server.py --port 4040"
  },
  "dev": { "url": "" },
  "prod": { "url": "" },
  "source": {
    "github": "https://github.com/jiahknee5/rapid-workflow",
    "gitlab": ""
  },
  "atlas": { "url": "" }
}
